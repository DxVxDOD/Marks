package components

import "Marks/internal/database"

templ BookmarksByUserID(bookmarks []database.Bookmark) {
	@layout() {
		<section>
			<div class="flex flex-wrap w-full border border-(--orange) p-3">
				for _,bookmark := range bookmarks {
					@card(bookmark)
				}
			</div>
		</section>
	}
}
